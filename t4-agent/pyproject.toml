[project]
name = "agenthon-t4-agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
analyze = "t4agent.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
